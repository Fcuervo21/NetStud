import { CREATE_POST, FETCH_ALL_POSTS } from "../constants/postsActions";

export const getPosts = () => async (dispatch) =>{
    try {
        // Information about the request
    } catch (error) {
        console.log(error);
    }
}

export const newPost = (postInfo) => async (dispatch) =>{
    try {
        // Information about the request
    } catch (error) {
        console.log(error);
    }
}